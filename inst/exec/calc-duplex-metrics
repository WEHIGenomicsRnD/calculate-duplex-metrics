#!/usr/bin/env sh

# Shell wrapper to run CalcDuplexMetrics::main() with a clean R session.
# Use --vanilla to avoid loading user or project profiles (e.g., .Rprofile/renv).

exec Rscript --vanilla -e "CalcDuplexMetrics::main()" -- "$@"
